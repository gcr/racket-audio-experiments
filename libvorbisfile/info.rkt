#lang setup/infotab
(define name "vorbisfile")
(define blurb
  '("Vorbisfile lets you decode the sound data from .OGG Vorbis files. This library provides a simple interface for that. Works with both (planet gcr/openal) and (planet clements/rsound)"))
(define primary-file "main.rkt")
(define categories '(media))
(define repositories '("4.x"))
(define scribblings '(("vorbisfile.scrbl" ())))
(define release-notes '((p "Initial commit.")))